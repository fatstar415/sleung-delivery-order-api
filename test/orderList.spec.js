const chai = require('chai')

describe('List order', () => {
  it('should execute validation against parameters')
  
  it('should throw error due to invalid inputs')
  
  it('should output response with empty array')

  it('should output response in correct format')
})